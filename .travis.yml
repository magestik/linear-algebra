language: cpp

addons:
  apt:
    packages:
    - libgtest-dev

matrix:
  include:
  - os: linux
    compiler: gcc
  - os: linux
    compiler: clang
  - os: osx
    compiler: clang

script: ctest -S ci/travis.cmake
